# Override parent config - use host target for tests
[build]
target = "x86_64-unknown-linux-gnu"

# NOTE: Cannot override [unstable] build-std here due to Cargo config merging rules
# The test runner (ndk test) must pass -Z build-std= to disable build-std

