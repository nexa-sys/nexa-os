# Override parent config - use host target for tests
[build]
target = "x86_64-unknown-linux-gnu"

# Explicitly disable build-std (tests use prebuilt std)
# Leave unstable section empty to override parent's build-std config
