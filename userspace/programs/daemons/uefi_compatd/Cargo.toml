[package]
name = "uefi_compatd"
version.workspace = true
edition.workspace = true

[[bin]]
name = "uefi-compatd"
path = "src/main.rs"

[dependencies]
nrlib = { workspace = true, optional = true, features = ["std"] }
nexa_boot_info.workspace = true

[features]
default = []
use-nrlib = ["nrlib", "nrlib/panic-handler"]
use-nrlib-std = ["nrlib"]
