# NexaOS 快速开始指南

欢迎使用 NexaOS！本指南将帮你在 5 分钟内完成环境设置和系统启动。

## 前置要求

### 系统要求
- Linux 系统（Ubuntu 20.04+、Debian 11+、Fedora 33+ 推荐）
- 4GB 内存和 2GB 硬盘空间
- Internet 连接（下载工具）

### 安装依赖

#### Ubuntu/Debian
```bash
sudo apt-get update
sudo apt-get install -y \
  build-essential \
  rustup \
  lld \
  grub-pc-bin \
  xorriso \
  qemu-system-x86 \
  mtools
```

#### Fedora/RHEL
```bash
sudo dnf install -y \
  @development-tools \
  rustup \
  lld \
  grub2-tools-efi \
  xorriso \
  qemu-system-x86 \
  mtools
```

#### macOS
```bash
brew install rustup lld grub xorriso qemu mtools
```

## 设置 Rust 环境

NexaOS 使用 Rust nightly 进行开发。设置如下：

```bash
# 安装 Rust nightly
rustup toolchain install nightly

# 在项目中启用 nightly
cd ~/dev/nexa-os  # 替换为实际路径
rustup override set nightly

# 安装所需组件
rustup component add rust-src llvm-tools-preview
```

## 快速构建和运行

### 完整系统构建（推荐）
```bash
cd ~/dev/nexa-os
./scripts/build-all.sh
```

这个脚本会自动：
1. ✅ 编译内核
2. ✅ 构建 initramfs（初始根文件系统）
3. ✅ 生成 ext2 根文件系统
4. ✅ 创建可启动的 ISO 镜像

构建完成后，所有输出文件在：
- 内核: `target/x86_64-nexaos/release/nexa-os`
- 根文件系统: `build/rootfs.ext2`
- 启动镜像: `build/initramfs.cpio`
- ISO 镜像: `dist/nexaos.iso`（如果配置了 GRUB）

### 在 QEMU 中运行
```bash
./scripts/run-qemu.sh
```

这会启动一个 QEMU 虚拟机，运行你构建的操作系统。

### 首次启动步骤

系统启动后，你会看到：
1. **GRUB 启动菜单** - 按 Enter 选择 NexaOS
2. **内核启动消息** - 显示引导进度
3. **Init 系统启动** - 显示 "Welcome to NexaOS"
4. **登录提示** - 输入用户名登录

### 默认登录凭据

**用户账户:**
- 用户名: `root` / 密码: `root`
- 用户名: `user` / 密码: `password`

## 与系统交互

### Shell 命令参考

登录后，你可以使用以下命令：

```bash
# 显示帮助信息
help

# 文件操作
ls                 # 列出文件
ls -a              # 列出所有文件（包括隐藏文件）
ls -l              # 详细列表
cat <file>         # 显示文件内容
pwd                # 显示当前路径

# 导航
cd <path>          # 切换目录
cd                 # 返回根目录

# 系统信息
uname -a           # 系统信息
whoami             # 显示当前用户
users              # 显示所有用户

# 用户管理
adduser <name>     # 创建用户
adduser -a <name>  # 创建管理员用户
login <user>       # 切换用户
logout             # 退出登录

# 工具
echo <text>        # 输出文本
clear              # 清屏
exit               # 退出 shell
```

### 高级编辑功能

Shell 支持以下键盘快捷键：

| 快捷键 | 功能 |
|--------|------|
| `Tab` | 自动补全命令和路径 |
| `Ctrl-C` | 取消当前输入 |
| `Ctrl-D` | 退出 shell（空行时） |
| `Ctrl-U` | 清空整行 |
| `Ctrl-W` | 删除前一个单词 |
| `Ctrl-L` | 清屏并重绘 |
| `Backspace` | 删除前一个字符 |

### 测试 Tab 补全

```bash
# 命令补全：输入 he + Tab，会补全为 help
he[Tab]

# 路径补全：输入 ls /b + Tab，会补全为 ls /bin/
ls /b[Tab]

# 多选项：输入 ls /b + Tab，会显示 /bin 和 /boot
ls /b[Tab]
```

## 故障排除

### QEMU 无法启动

**症状**: `qemu-system-x86_64: No machine type specified`

**解决**:
```bash
# 升级 QEMU
sudo apt-get install --only-upgrade qemu-system-x86

# 或禁用 KVM（如果硬件不支持）
# 编辑 scripts/run-qemu.sh，移除 -enable-kvm 选项
```

### 看不到输出

**症状**: QEMU 窗口黑屏无输出

**解决**:
```bash
# 检查 /tmp/qemu-serial.log 日志
tail -f /tmp/qemu-serial.log

# 从 scripts/run-qemu.sh 移除 -nographic 参数（如果有）
```

### 编译失败

**症状**: `error: linker 'lld' not found`

**解决**:
```bash
# 安装缺失的链接器
sudo apt-get install lld

# 或用 GCC 链接器替代
cargo build --release -Z unstable-options -C link-arg=-fuse-ld=bfd
```

### Rust 工具链问题

**症状**: `error: component 'rust-src' not found`

**解决**:
```bash
# 确保 nightly 已启用
rustup override set nightly

# 重新安装组件
rustup component add rust-src llvm-tools-preview --toolchain nightly
```

## 下一步

### 想要了解更多？
1. **系统概览** - 查看 [系统概览](系统概览.md)
2. **架构深入** - 阅读 [架构设计](架构设计.md)  
3. **Shell 命令** - 参考 [Shell 命令参考](shell与用户空间/命令参考.md)
4. **内核开发** - 学习 [内核开发指南](动态链接支持.md)

### 想要构建和测试？
1. **构建详解** - 查看 [构建流程](../en/BUILD-SYSTEM.md)（英文）
2. **测试指南** - 参考 [测试指南](../en/bugfixes/testing-guide.md)（英文）
3. **调试技巧** - 学习 [调试技巧](调试技巧.md)

### 想要贡献代码？
1. **功能清单** - 查看 [完成度报告](开发报告/完成度报告.md)
2. **编码规范** - 参考 [开发规范](开发规范.md)
3. **提交 PR** - 按照项目贡献指南提交

## 获取帮助

- **文档**: 查看 [NexaOS 中文文档中心](README.md)
- **问题**: 在 [GitHub Issues](https://github.com/nexa-sys/nexa-os/issues) 报告问题
- **讨论**: 在 [GitHub Discussions](https://github.com/nexa-sys/nexa-os/discussions) 讨论想法
- **反馈**: 欢迎提交拉取请求和建议

## 常见问题 (FAQ)

### Q: NexaOS 支持哪些硬件？
**A**: 目前支持 x86_64 架构的计算机。建议在 QEMU/KVM 或现代物理机上运行。ARM 支持正在计划中。

### Q: 能在真实硬件上运行吗？
**A**: 可以！不过需要通过 UEFI 或 BIOS 启动。建议先在虚拟机中测试。

### Q: 如何启用调试输出？
**A**: 在启动时将日志级别增加。查看 [调试技巧](调试技巧.md) 获取详细说明。

### Q: 支持网络吗？
**A**: 目前不支持网络功能。这是未来版本的计划功能。

### Q: 能运行 Linux 程序吗？
**A**: 部分可以。NexaOS 提供了 Linux ABI 兼容层，但兼容性仍在改进中。

### Q: 如何修改系统配置？
**A**: 编辑 `/etc/inittab` 文件配置初始化系统。查看 [inittab 配置指南](init系统/配置指南.md) 获取详细信息。

## 版本信息

- **NexaOS 版本**: 1.0.0 (2025-11-12)
- **Rust 工具链**: Nightly（见 `rust-toolchain.toml`）
- **内核架构**: 混合内核（Hybrid Kernel）
- **引导协议**: Multiboot2
- **目标平台**: x86_64

## 许可证

NexaOS 在 `LICENSE` 文件中指定的条款下发布。

---

**需要帮助？** 查看 [完整文档中心](README.md) 或在 GitHub 上提问！

祝你探索和开发愉快！🚀
