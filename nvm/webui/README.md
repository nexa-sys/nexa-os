# NVM WebUI

企业级虚拟化管理平台 Web 界面，使用 Vue 3 + TypeScript + Tailwind CSS 构建。

## 特性

- 🖥️ **虚拟机管理** - 创建、启动、停止、迁移虚拟机
- 📊 **实时监控** - CPU、内存、存储使用率图表
- 🌐 **网络管理** - 虚拟网络和桥接配置
- 💾 **存储管理** - 存储池和卷管理
- 🔄 **集群管理** - 多节点集群和高可用
- 📦 **模板系统** - VM 模板快速部署
- 🔐 **用户权限** - 基于角色的访问控制
- 🖱️ **Web 控制台** - VNC/SPICE 远程访问

## 技术栈

- **Vue 3.4** - 组合式 API + `<script setup>`
- **TypeScript 5.3** - 类型安全
- **Vite 5** - 快速构建工具
- **Tailwind CSS 3.4** - 实用优先的 CSS 框架
- **Pinia 2.1** - Vue 状态管理
- **Vue Router 4.2** - 路由管理
- **Chart.js + vue-chartjs** - 图表可视化
- **Axios** - HTTP 客户端

## 开发

### 环境要求

- Node.js 18+
- pnpm / npm / yarn

### 安装依赖

```bash
cd nvm/webui
npm install
```

### 启动开发服务器

```bash
npm run dev
```

开发服务器将在 http://localhost:5173 启动，并自动代理 API 请求到后端 (http://localhost:8006)。

### 构建生产版本

```bash
npm run build
```

构建产物将输出到 `dist/` 目录。

### 类型检查

```bash
npm run typecheck
```

### 代码检查

```bash
npm run lint
```

## 项目结构

```
webui/
├── public/              # 静态资源
├── src/
│   ├── api/            # API 客户端
│   ├── components/     # Vue 组件
│   │   ├── common/     # 通用组件
│   │   └── layout/     # 布局组件
│   ├── router/         # 路由配置
│   ├── stores/         # Pinia 状态管理
│   ├── styles/         # 全局样式
│   ├── views/          # 页面视图
│   ├── App.vue         # 根组件
│   └── main.ts         # 入口文件
├── index.html
├── package.json
├── tailwind.config.js
├── tsconfig.json
└── vite.config.ts
```

## 与后端集成

WebUI 通过 REST API 与 NVM 后端通信：

- **API Base**: `/api/v2`
- **WebSocket**: `/ws` (实时更新)
- **认证**: Bearer Token (JWT)

### 主要 API 端点

| 端点 | 方法 | 描述 |
|------|------|------|
| `/auth/login` | POST | 用户登录 |
| `/vms` | GET | 获取所有 VM |
| `/vms/:id` | GET | 获取单个 VM |
| `/vms/:id/start` | POST | 启动 VM |
| `/vms/:id/stop` | POST | 停止 VM |
| `/vms/:id/console` | GET | 获取控制台 URL |

## 部署

### 独立部署

构建后的静态文件可以通过任何 Web 服务器部署：

```bash
npm run build
# 将 dist/ 目录部署到 nginx/caddy/等
```

### 集成到 NVM 后端

WebUI 也可以直接集成到 NVM Rust 后端中，由后端提供静态文件服务。

## 许可证

MIT License - NexaOS Project
